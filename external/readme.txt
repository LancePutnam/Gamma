Installation:

OS X:
Copy include/*.h to /usr/local/include
Copy lib_osx/*.a to /usr/local/lib

The libraries are compiled as universal binaries so will work on both PPC and i386 architectures.


Windows:
Copy lib_win32/*.dll to c:\windows\system32